import React from 'react';

import Header from '../source/js/components/common/Header.js';

export default {
  title: 'Example/Header',
  component: Header,
};

export const Template = (args) => <Header {...args} />;
